import Foundation
import UIKit

class OfferStyle {
    static let minimumInteritemSpacing: CGFloat = 8 // Spacing between elements in the same row
    static let horizontalEdgePadding: CGFloat = 12 // Left and right padding on view content
    static let verticalEdgePadding: CGFloat = 24 // Top and bottom padding on view content
    static let lineSpacing: CGFloat = 24 // Spacing between rows of elements
}
